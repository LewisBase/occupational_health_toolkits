# OHTK 运行时依赖（精简版）
# 安装方式: pip install -r requirements.txt
# 训练 demo（titanic 教学脚本）需要 extras: pip install -e ".[training_demo]"

# --- 数值与数据 ---
numpy>=1.20
pandas>=1.3
scipy>=1.7
scikit-learn>=1.0

# --- 可视化（plot_helper.py 已废弃，seaborn/plotly/prophet 不再需要） ---
matplotlib>=3.5

# --- 建模 / 统计 ---
statsmodels>=0.13
lightgbm>=3.3
einops>=0.7
mlxtend>=0.23,<0.24
torch>=2.0

# --- 配置 / 工具 ---
pydantic>=2.0,<3.0
loguru>=0.6
pyfunctional>=1.5
tqdm>=4.60

# 注：以下包已从运行时依赖移除
#  - catboost, torchmetrics → [training_demo] extras（只用于 ohtk.training.train_model 的 titanic 教学 demo）
#  - prophet, plotly, seaborn → 完全移除（ohtk/utils/plot_helper.py 是死代码，0 处引用）
#  - setuptools → 移到 pyproject.toml [build-system].requires
